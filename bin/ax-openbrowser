#!/usr/bin/env bash
set -euo pipefail
cd "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export OPENBROWSER_CLI="${OPENBROWSER_CLI:-node /root/openbrowser/dist/index.js}"
exec python3 -m ax_browser_broker.adapters openbrowser "$@"
